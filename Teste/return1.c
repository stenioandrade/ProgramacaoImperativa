#include <stdio.h>
float return_1 (float n){
		if(n<0) return 1;
		else return 0;  
		}
int main (){
	float f;
	int r;
	scanf("%f",&f);
	r= returnn(f); 	
	printf("return %d",r);	
return r;
}
